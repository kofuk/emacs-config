(setq default-frame-alist '((width . 100)
                            (height . 35)
                            (cursor-type . bar)
                            (menu-bar-lines . 0)
                            (tool-bar-lines . 0)))

(setq frame-inhibit-implied-resize t)
